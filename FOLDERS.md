.
+ — containers
| + — // React containers
+ — components
| + — // React components
+ — reducers
| + — // Reducers
+ — actions
| + — // Actions
+ — types
| + — // Action types
+ — node_modules
| + — // Node modules
+ — webpack
| + — webpack.dev.config.js
+ — index.html
+ — index.js
+ — package.json
+ — README.md
+ — store.js
